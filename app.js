(async()=>{try{const parts=await Promise.all([1,2,3,4,5].map(i=>fetch(`./v2js${i}.txt?v=3`).then(r=>r.text())));const s=parts.join('').trim();const bytes=Uint8Array.from(atob(s),c=>c.charCodeAt(0));const ds=new DecompressionStream('gzip');const code=await new Response(new Blob([bytes]).stream().pipeThrough(ds)).text();(0,eval)(code)}catch(e){document.body.innerHTML='<div style="padding:30px;color:white;background:#07090d;font-family:-apple-system"><h2>万象工具箱启动失败</h2><p>请刷新页面或升级 iOS/Safari。</p><pre>'+String(e)+'</pre></div>'}})();
